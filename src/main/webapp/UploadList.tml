<html t:type="layout" title="upload.list.title"
      heading="message:display.heading" menu="AdminMenu"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd">

<div class="col-sm-10">
    <h2>${message:upload.list.heading}</h2>

    <p>Here is a list of your uploads:</p>

    <table class="table table-striped table-hover">
       <t:loop source="uploads" value="upload">
       	<tr>
       		<td><a href="${upload}" target="_blank">${upload}</a></td>
       	</tr>
       </t:loop>
    </table>

</div>

</html>
